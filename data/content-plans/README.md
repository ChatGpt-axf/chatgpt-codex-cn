# Content Plans

Content plans translate keyword research into a useful page brief. Before setting `status: approved`, confirm that nearby keywords do not share the same intent, core answer, and content structure. Plans may be passed to `npm run ai:draft -- --plan=<file>`, but generated files must remain `status: draft`, `draft: true`, and `noindex: true` until all audits and human review pass.
