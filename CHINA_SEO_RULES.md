# 中国搜索 SEO 规则

本项目把百度、360、搜狗和神马作为独立搜索引擎处理。Sitemap、站长平台提交或主动推送只是发现信号，不保证抓取、收录或排名。

## 中文页面基础

- Title 先表达页面要解决的中文问题，再放品牌；每个可索引页面必须唯一。
- Description 用自然中文概括答案、适用范围和限制，不堆叠关键词或同义词。
- H1 与页面主任务一致，H2/H3 暴露定义、步骤、比较、限制和验证方法。
- 在标题、Direct Answer、正文和锚文本中明确写出主题实体，避免连续使用“它”“这个”“该功能”而不说明对象。
- 使用自然同义词和符合中文阅读习惯的短句，不做机械分词、隐藏文本或关键词密度目标。
- URL 保持短、稳定、可读；现有英文 slug 不因中文站点而强制改写。变更已发布 URL 必须配置直接重定向。
- 图片按可见信息写中文 `alt`；装饰图使用空 `alt`。链接锚文本应说明目标内容，不使用大量相同的精确匹配词。

## 抓取与索引

- 主内容必须存在于静态 HTML，不能依赖客户端 JavaScript 执行后才出现。
- 公开页面允许 `Baiduspider`、`360Spider`、`Sogou web spider` 和 `YisouSpider`；后台、API、草稿、站内搜索和参数页按 `robots.txt` 规则限制。
- Sitemap 只包含自 canonical、可索引、非草稿、非重定向 URL，并为每项写入真实 `lastmod`。
- 空栏目、筛选组合和薄模板页默认 `noindex`，不进入 Sitemap。
- 重要页面需要可抓取的内部链接。核心内容应从首页或栏目页在合理点击层级内到达，并避免孤立页面。
- 更新时间只在实质复核后修改；页面应同时显示作者、发布日期、更新时间和事实来源。

## 移动与性能

- 每页包含移动 viewport，使用响应式布局，并保证正文、表格、按钮和导航在窄屏可用。
- 优先静态 HTML、压缩图片、稳定媒体尺寸、少量脚本和稳定服务器响应。
- 首屏不加载无关第三方代码；持续监测真实环境的响应状态、首屏速度和 Core Web Vitals。
- 神马准备度额外检查 viewport、HTML 体积、主内容可见性、canonical 和移动优先结构。

## 平台边界

- 百度：标准 URL-set Sitemap；普通收录 API 仅在已验证站点拥有合法 `BAIDU_SITE` 和 `BAIDU_TOKEN` 时启用。快速提交必须另有明确权限和端点。
- 360：标准 Sitemap、360 站长平台和人工提交清单；不假设存在公开通用提交 API。
- 搜狗：输出 XML 与 TXT Sitemap；站长平台的 Sitemap 能力可能取决于账号资格，不伪造自动提交。
- 神马：标准 Sitemap、神马站长平台和人工配置；重点检查移动端，不假设公开通用提交 API。

平台规则会变化。上线前应在各站长平台核对当前权限、配额和验证方式：[百度搜索资源平台](https://ziyuan.baidu.com/)、[360 站长平台](https://zhanzhang.so.com/)、[搜狗站长平台](https://data.open.sogou.com/)、[神马站长平台](https://zhanzhang.sm.cn/)。
